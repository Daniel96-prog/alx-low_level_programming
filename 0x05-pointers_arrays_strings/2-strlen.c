#include <string.h>
/**
*_strlen - returns length of string
*@s: input string
*@len: stores length of string s
*Return: length of string
*/
int _strlen(char *s)
{
int len = strlen(*s);
return (len);
}
